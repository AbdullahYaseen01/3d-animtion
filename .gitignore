node_modules
dist
dist-ssr
.vercel
.DS_Store
*.local
.env
.env.*
!.env.example

# QA output (screenshots/perf runs are regenerated; committed baselines live in docs/)
qa-output/
scripts/.tmp-*

# Large source media: only the optimized web film and the source loop are tracked
*.mp4
!assets-src/hero-loop.mp4
!public/media/*.mp4
